apiVersion: v1
kind: ConfigMap
metadata:
  name: expense-tracker-api-config
  namespace: expense-tracker
  labels:
    app: expense-tracker-api
data:
  PORT: "3000"
  NODE_ENV: "production"
  # BASE_URL set per environment (e.g. https://api.example.com)
